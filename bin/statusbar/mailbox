#!/bin/bash

# i3blocks mail module.
# Displays number of unread mail and an loading icon if updating.
# When clicked, brings up `neomutt`.

case $BLOCK_BUTTON in
	1) fspawn neomutt >/dev/null ;;
	2) setsid "${HOME}"/.config/mutt/etc/mailsync.sh >/dev/null & ;;
	3) pgrep -x dunst >/dev/null && notify-send "<b>📬 Mail module:</b>
- Shows unread mail
- Shows 🔃 if syncing mail
- Left click opens neomutt
- Middle click syncs mail" ;;
esac

cat /tmp/imapsyncicon_"${USER}" 2>/dev/null || echo "$(du -a ~/.local/share/mail/*/{INBOX,Inbox}/new/* 2>/dev/null | sed -n '$=')"
